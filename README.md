♾️ Focus Helper: ATC Edition

Languages: English | Português
🇬🇧 English Version

# A personal focus assistant that simulates an Air Traffic Control (ATC) tower to help you disconnect from the computer and avoid hyperfocus.

# Why does this project exist?

For many autistic and neurodivergent individuals, hyperfocus is a common experience. While it can be a superpower for productivity, it can also lead to burnout and neglecting basic needs. Generic "take a break" alerts are often jarring, ineffective, and easy to dismiss. Focus Helper offers a novel solution by transforming these interruptions into an engaging, immersive experience that respects the user's special interests.

By simulating an Air Traffic Control (ATC) tower, it uses a creative, aviation-themed approach to deliver critical alerts. Instead of a simple notification, users receive a "radio call" with urgent instructions, dynamically generated by the Llama 3.2 AI, making the process of disconnecting more effective and enjoyable.
✨ Key Features

Air Traffic Control (ATC) Simulation: Aviation-themed audio messages, generated in real-time by Llama 3.2 running locally via Ollama.

Progressive Hyperfocus Alerts: The system has multiple alert levels. The "emergency" intensifies as your continuous usage time increases, from a subtle pop-up to a critical radio call.

Realistic Voice and Radio Effects: Uses the Piper TTS engine for a high-quality local voice, processed with SoX to add filter and compression effects, and mixed with static noise for complete immersion.

Smart Home Integration: Can trigger webhooks for Home Assistant, allowing you to turn on warning lights, play alerts on Alexa, or create any other automation you can think of.

Configurable Wellbeing Questions: Asks dynamic questions about posture, hydration, and wellbeing, with different interaction types (Yes/No, Free Text).

Highly Configurable: Almost everything can be adjusted: alert timings, sounds, AI prompts, volumes, and the actions for each emergency level.

🛠️ How It Works

The application runs silently in the background, following a cycle:

Monitoring: Continuously checks mouse activity to calculate your uninterrupted usage time.

Detection: Compares your usage time with the configured thresholds for each alert level.

Execution: When a threshold is reached, the system triggers a sequence of actions defined for that level. This can include:

Showing a pop-up or desktop notification.

Playing a specific alert sound.

Generating text with Llama 3.2, synthesizing the voice with Piper, applying radio effects, and playing the final message.

Sending a webhook to Home Assistant.

🚀 Setup and Configuration
Prerequisites

This application orchestrates several command-line tools. You must install them first. On Debian/Ubuntu-based systems:

# Audio tools and build dependencies
sudo apt-get update
sudo apt-get install ffmpeg sox libasound2-dev libsox-fmt-mp3 mplayer

# Ollama (to run Llama 3.2)
# Follow the installation instructions at: https://ollama.com/
curl -fsSL https://ollama.com/install.sh | sh

# Piper (for local Text-to-Speech)
# Download the executable from: https://github.com/rhasspy/piper/releases
# Unzip it and place the 'piper' file in a directory in your PATH.

Project Setup

Download Llama 3.2 and a Piper Voice Model:

# Download the AI model
ollama pull llama3.2:latest

# Download a voice model for Piper (e.g., pt_BR-cadu-medium)
# Place the .onnx and .json files inside a 'voices' folder in the project.

Clone the Repository:

git clone https://github.com/your-username/focus-helper.git
cd focus-helper

File Structure:

Create an audio folder and place your alert sounds (alert_level_1.mp3, etc.) and radio_static.wav inside it.

Create a voices folder and place your Piper models in it.

Configure the Application:

Open the config/config.go file.

Adjust the timings, prompts, filenames, and your Home Assistant URL.

To test, you can use the loadDebugConfig() setting in main.go.

Install Go Dependencies:

go mod tidy

▶️ Usage

To run the application in development mode (with logs in the terminal):

go run .

To compile an executable for production:

# For Linux
go build -o focus-helper

# For Windows (without a console window)
go build -ldflags="-H windowsgui" -o focus-helper.exe

🇧🇷 Versão em Português

# Um assistente de foco pessoal que simula uma torre de comando de aeroporto (ATC) para ajudar a desconectar do computador e evitar o hiperfoco.

# Por que este projeto existe?

Para muitos indivíduos autistas e neurodivergentes, o hiperfoco é uma experiência comum. Embora possa ser um superpoder para a produtividade, também pode levar ao esgotamento e à negligência de necessidades básicas. Alertas genéricos de "faça uma pausa" são frequentemente disruptivos, ineficazes e fáceis de ignorar. O Focus Helper oferece uma solução inovadora ao transformar essas interrupções em uma experiência imersiva e engajante, que respeita os interesses especiais do usuário.

Ao simular uma torre de comando de tráfego aéreo (ATC), ele usa uma abordagem criativa com temática de aviação para entregar alertas críticos. Em vez de uma simples notificação, os usuários recebem uma "chamada de rádio" com instruções urgentes, geradas dinamicamente pela IA Llama 3.2, tornando o processo de desconexão mais eficaz e agradável.
✨ Funcionalidades Principais

Simulação de Torre de Comando (ATC): Mensagens de áudio com temática de aviação, geradas em tempo real pelo Llama 3.2 rodando localmente via Ollama.

Alertas de Hiperfoco Progressivos: O sistema possui múltiplos níveis de alerta. A "emergência" se intensifica à medida que o tempo de uso contínuo aumenta, desde um pop-up sutil até uma chamada de rádio crítica.

Voz e Efeitos de Rádio Realistas: Utiliza o motor Piper TTS para uma voz local de alta qualidade, processada com SoX para adicionar efeitos de filtro e compressão, e mixada com ruído estático para uma imersão completa.

Integração com Casa Inteligente: Pode disparar webhooks para o Home Assistant, permitindo acender luzes de alerta, tocar avisos na Alexa ou qualquer outra automação que você criar.

Perguntas de Bem-Estar Configuráveis: Faz perguntas dinâmicas sobre postura, hidratação e bem-estar, com diferentes tipos de interação (Sim/Não, Texto Livre).

Altamente Configurável: Praticamente tudo pode ser ajustado: os tempos de alerta, os sons, os prompts da IA, os volumes e as ações para cada nível de emergência.

🛠️ Como Funciona

A aplicação roda silenciosamente em segundo plano, seguindo um ciclo:

Monitoramento: Verifica continuamente a atividade do mouse para calcular seu tempo de uso ininterrupto.

Detecção: Compara seu tempo de uso com os limites definidos na configuração para cada nível de alerta.

Execução: Quando um limite é atingido, o sistema dispara uma sequência de ações definidas para aquele nível. Isso pode incluir:

Mostrar um pop-up ou notificação de desktop.

Tocar um som de alerta específico.

Gerar um texto com o Llama 3.2, sintetizar a voz com o Piper, aplicar efeitos de rádio e tocar a mensagem final.

Enviar um webhook para o Home Assistant.

🚀 Instalação e Configuração
Pré-requisitos

Esta aplicação orquestra várias ferramentas de linha de comando. Você precisa instalá-las primeiro. Em sistemas baseados em Debian/Ubuntu:

# Ferramentas de áudio e dependências de compilação
sudo apt-get update
sudo apt-get install ffmpeg sox libasound2-dev libsox-fmt-mp3 mplayer

# Ollama (para rodar o Llama 3.2)
# Siga as instruções de instalação em: https://ollama.com/
curl -fsSL https://ollama.com/install.sh | sh

# Piper (para Text-to-Speech local)
# Baixe o executável em: https://github.com/rhasspy/piper/releases
# Descompacte e coloque o arquivo 'piper' em um diretório do seu PATH.

Configuração do Projeto

Baixe o Llama 3.2 e um Modelo de Voz Piper:

# Baixa o modelo de IA
ollama pull llama3.2:latest

# Baixe um modelo de voz para o Piper (ex: pt_BR-cadu-medium)
# Coloque os arquivos .onnx e .json dentro de uma pasta 'voices' no projeto.

Clone o Repositório:

git clone https://github.com/seu-usuario/focus-helper.git
cd focus-helper

Estrutura de Arquivos:

Crie uma pasta audio e coloque seus sons de alerta (alert_level_1.mp3, etc.) e o radio_static.wav dentro dela.

Crie a pasta voices e coloque seus modelos do Piper nela.

Configure a Aplicação:

Abra o arquivo config/config.go.

Ajuste os tempos, prompts, nomes de arquivos e a URL do seu Home Assistant.

Para testar, você pode usar a configuração loadDebugConfig() no main.go.

Instale as Dependências do Go:

go mod tidy

▶️ Uso

Para rodar a aplicação em modo de desenvolvimento (com logs no terminal):

go run .

Para compilar um executável para produção:

# Para Linux
go build -o focus-helper

# Para Windows (sem abrir um console)
go build -ldflags="-H windowsgui" -o focus-helper.exe
