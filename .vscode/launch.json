{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CGo Program",
            "type": "go",
            "request": "launch",
            "mode": "exec",
            "program": "${workspaceFolder}/focus-helper-debug -debug",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build for Debugging"
        }
    ]
}